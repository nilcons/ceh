#!/bin/bash -e

. /opt/ceh/scripts/common-functions.sh

if [ -t 1 ]; then
    export CEH_INSTALL_AUTOHASH=1
    echo >&2 "*** Running on a TTY, autoguessing hashes"
fi

ceh_nixpkgs_install_for_emacs "$@"
